// =====================================================
// package.json
// =====================================================
{
  "name": "res-backend",
  "version": "1.0.0",
  "description": "RES Management System Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "sequelize": "^6.35.0",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5",
    "nodemailer": "^6.9.7",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}

// =====================================================
// .env (пример для локальной разработки)
// =====================================================
DATABASE_URL=postgres://username:password@localhost:5432/resdb
JWT_SECRET=your-super-secret-jwt-key-change-this
NODE_ENV=development
MAIL_HOST=smtp.mail.ru
MAIL_PORT=465
MAIL_USER=your-email@mail.ru
MAIL_PASS=your-app-password

// =====================================================
// .gitignore
// =====================================================
node_modules/
.env
uploads/
*.log
.DS_Store
